import { SelectdefautDirective } from './selectdefaut.directive';

describe('SelectdefautDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectdefautDirective();
    expect(directive).toBeTruthy();
  });
});
