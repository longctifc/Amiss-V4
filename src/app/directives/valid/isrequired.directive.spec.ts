import { IsrequiredDirective } from './isrequired.directive';

describe('IsrequiredDirective', () => {
  it('should create an instance', () => {
    const directive = new IsrequiredDirective();
    expect(directive).toBeTruthy();
  });
});
